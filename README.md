# 0G-Node-connecting
Below is a ready-to-run Python script that connects to a 0G (ZeroGravity) node via its JSON-RPC endpoint, checks the node status, and fetches the latest block number. It works for both 0G Storage and 0G AI networks (mainnet / testnet) – just change the RPC URL.
